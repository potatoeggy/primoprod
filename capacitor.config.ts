import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "me.eggworld.primoprod",
  appName: "primoprod",
  webDir: "dist",
  npmClient: "yarn",
};

export default config;
